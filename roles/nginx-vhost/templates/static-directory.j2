{% if l.location != '/' and (l.retain_path is defined and not l.retain_path) %}
rewrite {{ l.location }}(/?(.*)) /$2  break;
{% endif %}
index index.html;
try_files "${uri}.html" $uri $uri/ =404;

root {{ l.static_directory }};

