{
  "name": "@cenk1cenk2/ansible-roles",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:cenk1cenk2/ansible-roles.git",
  "author": "Cenk Kılıç <cenk@kilic.dev>",
  "license": "MIT",
  "private": true,
  "root": true,
  "type": "module",
  "scripts": {
    "format": "ansible-lint -v --write && prettier --loglevel warn --write .",
    "lint": "ansible-lint -v && prettier  --loglevel warn --check ."
  },
  "lint-staged": {
    "*.{yml,yml.j2,json}": [
      "prettier --loglevel warn --write"
    ]
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm exec lint-staged",
    "pre-push": "pnpm run lint"
  },
  "devDependencies": {
    "@cenk1cenk2/cz-cc": "^1.6.6",
    "lint-staged": "^13.2.3",
    "prettier": "^2.8.8",
    "simple-git-hooks": "^2.8.1"
  }
}
